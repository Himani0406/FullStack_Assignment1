{
  "name": "assignment-1",
  "version": "1.0.0",
  "description": "Assignment 1",
  "main": "server.js",
  "scripts": {
    "start": "npm rum build && npm run serve",
    "serve": "nodemon server\\server.js",
    "build": "babel src --presets react,es2015 --out-dir public",
    "watch": "nodemon --watch src -e jsx --exec npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Himani",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "nodemon": "^2.0.2"
  }
}
